# 💎 دليل البطاقة الترويجية - Promo Card

## 📋 نظرة عامة

بطاقة ترويجية احترافية تظهر في الزاوية اليمنى السفلية من الموقع، مصممة لجذب الانتباه وتحفيز الزوار على التواصل.

---

## ✨ المميزات

### 🎨 التصميم:
- ✅ تصميم عصري بألوان جذابة
- ✅ Gradient background في البانر
- ✅ أيقونة متحركة (pulse animation)
- ✅ زر CTA بارز بـ hover effect
- ✅ Badge للخصم (73%)
- ✅ 3 مميزات بأيقونات

### 🚀 الوظائف:
- ✅ Slide-in animation عند الظهور
- ✅ Slide-out animation عند الإغلاق
- ✅ حفظ حالة الإغلاق (localStorage)
- ✅ لا تظهر مرة أخرى بعد الإغلاق
- ✅ Responsive كامل (Desktop + Tablet + Mobile)

### 📱 التوافق:
- ✅ جميع الشاشات (من 320px إلى 4K)
- ✅ لا تتداخل مع الهيدر أو الفوتر
- ✅ z-index: 100002 (فوق كل شيء)

---

## 🚀 طريقة التركيب

### الخطوة 1: تأكد من تحميل الملفات الأساسية

في **Settings → Code → Head**، تأكد من وجود:

```html
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
<link rel="stylesheet" href="https://raw.githack.com/biqc-s/asir-tour/refs/heads/claude/debug-carrd-integration-fOtCT/styles.css">
<script src="https://raw.githack.com/biqc-s/asir-tour/refs/heads/claude/debug-carrd-integration-fOtCT/script.js"></script>
```

### الخطوة 2: إضافة البطاقة الترويجية

1. في صفحة التحرير، اضغط **➕ Add Element**
2. اختر **Embed** → **Code**
3. Style: **Inline**
4. الصق محتوى ملف `promo-card.html`
5. احفظ وانشر

---

## 🔧 التعديلات المطلوبة

افتح ملف `promo-card.html` وعدّل:

### 1. اسم الشركة/الخدمة
```html
<span>✨ عرض بيكارم الخاص</span>
```
ضع اسم شركتك أو عنوان العرض

### 2. النص الترويجي
```html
<h3 class="promo-title">موقعك الاحترافي بانتظارك!</h3>
<p class="promo-description">انطلق بهوية رقمية رسمية وموثوقة</p>
```
عدّل النصوص حسب عرضك

### 3. السعر
```html
<span class="price-new">399 ريال</span>
<span class="price-old">1,500 ريال</span>
```
ضع السعر الجديد والقديم

### 4. نسبة الخصم
```html
<span class="price-badge">خصم 73%</span>
```
سيتم حسابها تلقائياً: `((1500 - 399) / 1500) × 100 = 73%`

### 5. رقم الواتساب
```html
<a href="https://wa.me/966XXXXXXXXX"
```
ضع رقمك (مثال: 966501234567)

### 6. المميزات
```html
<div class="feature-item">
  <i class="fas fa-check-circle"></i>
  <span>تصميم احترافي</span>
</div>
```
عدّل المميزات الثلاثة حسب عرضك

---

## 🎨 تخصيص الألوان

الألوان تستخدم CSS Variables من ملف styles.css:

```css
:root {
  --primary-color: #1B365D;    /* لون البانر والزر */
  --secondary-color: #8B2635;   /* لون الـ badge */
  --wa-color: #27A148;          /* لون السعر */
}
```

لتغيير الألوان:
1. افتح `styles.css`
2. عدّل القيم في `:root`
3. push للـ GitHub

---

## 💡 التخصيصات الإضافية

### تغيير موقع البطاقة

في `styles.css`، عدّل:

```css
.promo-box {
  bottom: 30px;  /* المسافة من الأسفل */
  right: 30px;   /* المسافة من اليمين */
}
```

**بدائل:**
- `left: 30px;` - لوضعها في اليسار
- `top: 90px;` - لوضعها في الأعلى (تحت الهيدر)

### تغيير حجم البطاقة

```css
.promo-box {
  width: 330px;  /* عرض البطاقة */
}
```

### تعطيل الـ localStorage (تظهر في كل مرة)

في `script.js`، احذف أو علّق هذه الأسطر:

```javascript
// حفظ حالة الإغلاق في localStorage
localStorage.setItem('promoCardClosed', 'true');

// والجزء الذي يتحقق من localStorage
const wasClosed = localStorage.getItem('promoCardClosed');
if (wasClosed === 'true') {
  promoBox.style.display = 'none';
}
```

### تغيير مدة الأنيميشن

في `styles.css`:

```css
.promo-box {
  animation: slideInUp 0.5s ease-out;  /* 0.5s = نصف ثانية */
}
```

جرب: `0.3s` (أسرع) أو `0.8s` (أبطأ)

---

## 🧪 الاختبار

### 1. اختبار الظهور

افتح موقعك، يجب أن:
- ✅ تظهر البطاقة في الزاوية اليمنى السفلية
- ✅ أنيميشن الظهور يعمل (slide up)
- ✅ الأيقونة تتحرك (pulse)

### 2. اختبار الإغلاق

اضغط على زر ✕:
- ✅ أنيميشن الإخفاء يعمل (slide down)
- ✅ البطاقة تختفي
- ✅ Console يظهر: `✅ تم إغلاق البطاقة الترويجية`

### 3. اختبار localStorage

1. أغلق البطاقة
2. اعمل Refresh للصفحة
3. يجب أن **لا تظهر** البطاقة مرة أخرى

**لإعادة ظهورها:**
- افتح Console (F12)
- اكتب: `localStorage.removeItem('promoCardClosed')`
- اعمل Refresh

### 4. اختبار Responsive

جرب الموقع على:
- 📱 Mobile (320px - 480px)
- 📱 Tablet (481px - 768px)
- 💻 Desktop (769px+)

يجب أن تتكيف البطاقة مع كل حجم شاشة.

---

## 📱 Responsive Behavior

| حجم الشاشة | عرض البطاقة | الموقع |
|------------|-------------|---------|
| Desktop (> 768px) | 330px | أسفل يمين |
| Mobile (< 480px) | 92% | في المنتصف |
| Very Small (< 360px) | 92% | في المنتصف |

---

## 🐛 استكشاف الأخطاء

### المشكلة: البطاقة لا تظهر

**الحل:**
1. افتح Console (F12)
2. ابحث عن رسالة: `💎 تم عرض البطاقة الترويجية`
3. إذا لم تظهر، تأكد من:
   - CSS محمّل صحيح
   - JavaScript محمّل صحيح
   - الـ embed موجود في الصفحة

### المشكلة: زر الإغلاق لا يعمل

**الحل:**
1. تأكد أن JavaScript محمّل في Head
2. افتح Console وابحث عن أخطاء
3. تأكد من وجود دالة `closePromo()` في script.js

### المشكلة: البطاقة تظهر دائماً (حتى بعد الإغلاق)

**الحل:**
1. تأكد أن localStorage مفعّل في المتصفح
2. تأكد من عدم فتح الموقع في Incognito/Private mode
3. افتح Console واكتب: `localStorage.getItem('promoCardClosed')`
   - إذا كانت النتيجة `null`، المشكلة في حفظ الحالة

### المشكلة: البطاقة تغطي الفوتر

**الحل:**
عدّل في `styles.css`:
```css
.promo-box {
  bottom: 90px;  /* زد المسافة */
}
```

---

## 🎯 أفكار للتطوير

### 1. إضافة عداد تنازلي

```html
<div class="countdown">
  <i class="fas fa-clock"></i>
  <span>ينتهي العرض خلال: <strong>48 ساعة</strong></span>
</div>
```

### 2. إضافة شهادات العملاء

```html
<div class="testimonial">
  <p>"موقع احترافي وخدمة ممتازة!"</p>
  <small>- أحمد، عميل سابق</small>
</div>
```

### 3. إضافة أرقام إحصائيات

```html
<div class="stats">
  <div class="stat-item">
    <strong>500+</strong>
    <span>عميل راضٍ</span>
  </div>
</div>
```

### 4. تأخير الظهور

في `script.js`، أضف:

```javascript
// إظهار البطاقة بعد 3 ثوان
setTimeout(function() {
  const promoBox = document.getElementById('promoCard');
  if (promoBox) {
    promoBox.style.display = 'block';
  }
}, 3000);
```

---

## 📊 قياس الأداء

### Tracking النقرات

أضف في `script.js`:

```javascript
const promoBtn = document.querySelector('.promo-cta-btn');
if (promoBtn) {
  promoBtn.addEventListener('click', function() {
    console.log('🎯 تم الضغط على زر العرض');
    // يمكن إضافة Google Analytics هنا
  });
}
```

---

## 🔗 الملفات المستخدمة

| الملف | الوظيفة | الحجم |
|-------|---------|-------|
| `promo-card.html` | HTML للبطاقة | ~2 KB |
| `styles.css` | CSS (القسم الترويجي) | ~6 KB |
| `script.js` | JavaScript (closePromo) | ~1 KB |

---

## 💰 نصائح لزيادة التحويلات

1. **استخدم أرقام دقيقة**: `399 ريال` أفضل من `400 ريال`
2. **اذكر نسبة الخصم**: يجذب الانتباه
3. **أضف عنصر الاستعجال**: "العرض ينتهي قريباً"
4. **اذكر الضمان**: "استرجاع مجاني خلال 30 يوم"
5. **استخدم أرقام اجتماعية**: "انضم لـ 500+ عميل"

---

## 🚀 الإطلاق

### قبل النشر:

- [ ] عدّل جميع النصوص
- [ ] ضع رقم واتساب صحيح
- [ ] جرب الزر للتأكد من عمل الرابط
- [ ] اختبر على أجهزة مختلفة
- [ ] تأكد من localStorage يعمل

### بعد النشر:

- [ ] راقب عدد النقرات
- [ ] اسأل العملاء عن تجربتهم
- [ ] عدّل السعر/النص حسب الحاجة
- [ ] جرب مواقع مختلفة للبطاقة (A/B testing)

---

## 📞 الدعم

إذا واجهت مشكلة:
1. افتح Console (F12)
2. خذ screenshot من الأخطاء
3. راجع قسم "استكشاف الأخطاء" أعلاه

---

**تم بواسطة Claude** 🤖

آخر تحديث: 2026-01-19
