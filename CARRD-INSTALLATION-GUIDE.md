# 📘 دليل تركيب الهيدر في Carrd.co

## ⚠️ المشكلة
Carrd لا يدعم JavaScript في الـ **Inline embeds** لذلك يجب فصل HTML عن JavaScript.

---

## ✅ الحل: خطوتان بسيطتان

### **الخطوة 1️⃣: إضافة JavaScript في Head**

1. افتح موقعك في Carrd
2. اذهب إلى **⚙️ Settings** (الإعدادات)
3. اختر **</> Code**
4. في قسم **Head**، الصق كود JavaScript من ملف `header-javascript.html`
5. احفظ

---

### **الخطوة 2️⃣: إضافة HTML في الصفحة**

1. في صفحة التحرير، اضغط **➕ Add Element**
2. اختر **Embed** → **Code**
3. اختر Style: **Inline**
4. الصق كود HTML من ملف `header-html-only.html`
5. احفظ وانشر (**Publish**)

---

## 🔧 التعديلات المطلوبة قبل الاستخدام

افتح ملف `header-html-only.html` وعدّل:

### 1. اسم المرشد
```html
<span class="guide-name">عبدالله السلمان</span>
```
ضع اسمك بدلاً من "عبدالله السلمان"

### 2. رقم الترخيص
```html
<span class="lic-number">12345678</span>
```
ضع رقم ترخيصك بدلاً من "12345678"

### 3. رقم الواتساب
```html
<a href="https://wa.me/966XXXXXXXXX"
```
ضع رقمك بدلاً من "966XXXXXXXXX" (مثال: 966501234567)

---

## 🧪 اختبار الكود

بعد النشر، افتح موقعك:

1. **افتح Console في المتصفح**:
   - **Chrome/Edge**: اضغط `F12` → اختر **Console**
   - **Safari**: اضغط `Cmd+Option+C`
   - **Firefox**: اضغط `F12` → اختر **Console**

2. **ابحث عن هذه الرسائل**:
   ```
   🚀 تم تحميل السكريبت بنجاح
   ✅ تم العثور على جميع العناصر
   🎉 تم تفعيل جميع الأزرار بنجاح
   ```

3. **جرب الضغط على زر القائمة**:
   - يجب أن تظهر رسالة: `📂 تم فتح القائمة`
   - عند الإغلاق: `🔒 تم إغلاق القائمة`

---

## ❓ إذا لم يعمل

### إذا ظهرت رسالة خطأ في Console:

**`❌ لم يتم العثور على العناصر المطلوبة`**
- تأكد أنك أضفت HTML في **Inline embed** وليس Head
- تأكد أنك أضفت JavaScript في **Head** وليس Inline

**`TypeError: Cannot read property 'addEventListener'`**
- JavaScript يتم تحميله قبل HTML
- تأكد أن JavaScript في Head فقط

---

## 📂 الملفات المستخدمة

| الملف | الاستخدام | المكان في Carrd |
|-------|-----------|------------------|
| `header-javascript.html` | كود JavaScript | Settings → Code → **Head** |
| `header-html-only.html` | كود HTML فقط | Add Element → Embed → **Inline** |
| `styles.css` | يتم تحميله تلقائياً من رابط GitHack | لا يحتاج إضافة يدوية |

---

## 🎨 تخصيص الألوان

إذا أردت تغيير الألوان، عدّل في ملف `styles.css`:

```css
:root {
  --primary-color: #1B365D;    /* اللون الأساسي (أزرق) */
  --secondary-color: #8B2635;   /* اللون الثانوي (أحمر) */
  --wa-color: #27A148;          /* لون الواتساب (أخضر) */
}
```

بعد التعديل، سوّي push للكود:
```bash
git add styles.css
git commit -m "تحديث الألوان"
git push origin claude/debug-carrd-integration-fOtCT
```

---

## 🔗 روابط مفيدة

- **رابط CSS**: https://raw.githack.com/biqc-s/asir-tour/refs/heads/claude/debug-carrd-integration-fOtCT/styles.css
- **موقعك**: https://jahash.carrd.co/
- **GitHub Repo**: https://github.com/biqc-s/asir-tour

---

## ✨ مميزات الهيدر

- ✅ Header ثابت (Fixed)
- ✅ Sidebar منزلق من اليمين
- ✅ Overlay شفاف للخلفية
- ✅ زر واتساب مباشر
- ✅ بطاقة الترخيص
- ✅ Responsive (يتكيف مع جميع الشاشات)
- ✅ RTL Support (دعم العربية)
- ✅ Smooth Animations

---

## 💬 إذا احتجت مساعدة

1. افتح Console في المتصفح
2. اضغط على زر القائمة
3. خذ screenshot من الـ Console
4. شارك الـ screenshot لأساعدك

---

**تم إنشاء هذا الدليل بواسطة Claude** 🤖
